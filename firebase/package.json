{
  "name": "just-some-stars-firebase-rules",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project jss-rules-test --config firebase.json \"node --test test/firestore.rules.test.mjs\"",
    "test:functions": "pnpm --dir functions test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.18.0",
    "firebase-tools": "15.28.2"
  }
}
