# RevenueCat Unity package provenance

Verified: 2026-08-31

The archive in this directory is stored byte-for-byte as published by the
RevenueCat project through OpenUPM. It was not extracted, repacked or modified.

| Repository archive | Published source | Bytes | SHA-256 |
|---|---|---:|---|
| `com.revenuecat.purchases-unity-9.9.1.tgz` | `https://package.openupm.com/com.revenuecat.purchases-unity/-/com.revenuecat.purchases-unity-9.9.1.tgz` | 67939 | `6014a539443b8c2f2c1baf834476957ee891e06730781a1574efbffd2333d313` |

The package metadata identifies version `9.9.1`, official repository
`https://github.com/RevenueCat/purchases-unity`, and revision
`a8e805ecde13f2fc9aad711ea64bd7ede9deafed`.

Verification procedure:

1. Download the published archive into a temporary directory.
2. Compare its byte count and SHA-256 with the repository archive.
3. Require byte-for-byte identity before accepting an update.
4. Run the focused package, entitlement, shop and Android build gates.

The public RevenueCat SDK key is supplied only through the ignored,
transactional build configuration lease. No RevenueCat key belongs in this
archive, this provenance record, Git, or a build log.
