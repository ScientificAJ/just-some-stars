# Just Some Stars issue register

This register holds findings that are real but outside the frozen acceptance
contract of the task currently being executed.

## Operating rule

- An out-of-scope finding is recorded here; it does not expand, block, or
  restart the current task.
- Severity does not automatically promote a finding into the current task.
- A finding is worked only when the user explicitly promotes it or its named
  owner task begins.
- Critics report against the frozen task contract once. Newly observed
  out-of-scope findings are appended here instead of starting another audit
  loop.
- Focused verification belongs inside implementation. Full regression runs
  happen once at the declared package/release boundary.

## Open findings

| ID | Finding | Owner / revisit point | Current-task effect |
|---|---|---|---|
| JSS-001 | Codemagic app/repository/workflow setup is complete, with 500 free macOS minutes available. The user approved deferring remote Unity execution because the account has Unity Personal and no Plus/Pro serial for CI activation. | Revisit only if a valid Unity CI license becomes available | None; do not spend build minutes |
| JSS-002 | Produce the signed Google Play AAB, begin closed testing, and create the Galaxy Seller application record when a playable release candidate exists. | Growth and release package (Tasks 31–33) | None |
| JSS-003 | Replace disabled Continue with the real navigation/content flow. Task 6's Settings/input, Task 7's recoverable saves and Task 8's mode/streaming foundations are complete; the committed Task 8 catalogue remains honestly empty until real destinations exist. | Later Frontend/content integration | None |
| JSS-005 | Investigate the two un-attributed persistent native allocations reported only during development-player process shutdown if they recur under stack-enabled profiling. | Later device/performance QA | None |
| JSS-006 | Improve Unity-canvas device automation discovery so Argent does not require the documented logical-coordinate fallback. On the Task 13 Limrun target, Argent 0.21 two-pointer injection delivered pointer action `5` as an unsupported pen event, raw recordings were `720x1616` while ADB input correctly used logical `1616x720`, stationary same-point ADB swipes were ignored while real drags ramped the virtual stick, and the shared server required the repository-bundled FFmpeg on its process PATH. | Tooling/QA maintenance | None; Task 13 uses measured one-pixel single-touch logical-landscape input and the bundled encoder without changing the toolchain |
| JSS-007 | Replace Task-specific CI test counts and smoke filters as later tasks expand or rename the suites. | Task 31 CI hardening | None |
| JSS-008 | Replace Task 8's honest `Frontend` streaming-failure fallback with the real Clubhouse scene once that ship-hub scene exists. | Task 26 Clubhouse integration | None; Task 8 must not ship a fake hub scene |
| JSS-009 | If typed events later publish across worker threads, define and test the stronger contract for a subscriber being disposed while its snapshotted callback is already in flight. | First cross-thread event producer | None; Task 9 guarantees type-safe snapshot dispatch and idempotent removal, not cancellation of an already-running callback |
| JSS-010 | Consider introducing an `IGameEvent` marker only if arbitrary non-game payloads are actually published through the generic bus. | First event-vocabulary expansion | None; current callers and published contracts use only the six typed game events |
| JSS-013 | Decide whether to retain, move to an archival branch, or delete the uncommitted Task 12 full-3D Captain source, textures, scripts and evidence after the 2.5D pipeline is proven. | After replacement Task 12 Stage 5 passes | None; preserve the files and do not resume Stage 4 rigging |
| JSS-017 | Argent 0.22.1 is available while the project remains pinned to the verified 0.21.0 device-QA setup. | Task 31 tooling hardening or an explicit user-approved maintenance pass | None; do not change the inspection stack during Task 13 acceptance |
| JSS-018 | `CaptainSpriteCompatibilityTests` still expects 30 physical `.spriteatlas` files, while the accepted 2.5D pipeline now publishes the real PNG textures/manifests directly and has removed those stale atlas wrappers and Addressables entries. The focused class is `3/4`; the current PNG importer is `6/6`, the actual layered renderer is `3/3`, and the rebuilt Android player renders the package correctly. | Next focused 2D asset-pipeline maintenance pass, before the next package-boundary regression | None for the bounded lower-leg correction; update the stale compatibility assertion or restore a genuinely consumed atlas contract, but do not preserve unused wrappers merely to satisfy the old count |
| JSS-021 | Activate the checked-in Task 21–22 account/cloud architecture against a real Firebase project: register both Android package IDs and signing fingerprints; add the ignored client configuration; choose and integrate maintained Google ID-token and Firebase callable-function transports; add/pin the official Functions client package if that transport is chosen; wire concrete Firebase Auth/Firestore/`IBirthdayGiftGateway`/`IAccountDeletionGateway` implementations; implement an App-Check-protected server-authoritative first-profile bootstrap because Firestore clients are intentionally denied both root create and delete; implement and integration-test typed update/server-timestamp plus server-owned-birthday-field preservation; build and deploy the reviewed rules, indexes and callables with App Check enforcement/attestation; verify the server-authoritative delete path removes Firebase Auth before the UID root and that the Auth deletion trigger cleans retry residue; then prove link/merge/checkpoint sync/conflict/export/unlink/delete and one-only annual grant/account deletion across two signed physical Android installs. The current checked-in build intentionally injects unavailable gateways, remains fully playable offline, and must not imply that adding `google-services.json` alone activates cloud backup or birthday gifts. | External Firebase/OAuth activation pass before account-dependent release testing | None for Tasks 22–30 local work; do not fabricate credentials, deployment or second-device evidence |
| JSS-023 | Activate the checked-in Task 23 RevenueCat/Google commerce boundary: add the primary Android app to RevenueCat, create the six exact products/packages/entitlements and `launch_cosmetics` offering, review transfer behavior, inject the Test Store key only through the build lease, complete purchase/cancel/interruption/restore and anonymous-to-linked identity tests, create matching Google Play products, produce/upload a signed billing-enabled AAB, complete a licensed Google purchase, and record the matching RevenueCat customer, transaction, verified entitlement and restoration without committing keys, receipts or account data. | Credentialed RevenueCat and Google Play activation before release commerce sign-off | None for Tasks 24–30 local work; the full story remains available and the optional store fails closed when unconfigured |
| JSS-024 | Activate the checked-in Task 24 Samsung Galaxy commerce boundary: accept the Samsung IAP terms under the owner account, complete Seller/commercial prerequisites, create and price the six exact permanent Item products, deploy the authenticated receipt verifier, inject its non-secret runtime endpoint/configuration without committing credentials, resolve Samsung IAP 6.5.2 in a signed Galaxy AAB, inspect the final dependency/permission/package/mode graph, and prove TEST, TEST_FAILURE and closed-beta PRODUCTION purchase/cancel/interruption/restore/identity/acknowledgement behavior on a licensed physical Samsung device. | Credentialed Samsung Seller/verifier/physical-device activation before Galaxy commerce sign-off | None for Tasks 25–30 local work; the checked-in verifier grants nothing and the full story remains playable |
| JSS-025 | Run one corrected exact-APK Task 25 spot-check from production Vesper flight through Koro completion and reload when hosted-device capacity is available. The final source-fresh production E2E already exercises the same real flight model, landing sequence, destination scene, Lens scans, comparison, geyser rhythm, fragment recovery and durable save; the earlier paid run predates the final visual/progression corrections and is not relabeled. | Task 30 broad device/performance acceptance | None for Task 25 local acceptance; do not fabricate a final-device claim |

## Completed tasks

| Task | Completed | Evidence |
|---|---|---|
| Award-level Frontend redesign | 2026-08-22 — user approved | Approved landscape main screen and all local panels; EditMode `211/211`; PlayMode `70/70`; internal APK SHA-256 `9e85f4ec24d20c62a786997ac4a42dab132a79118e401f2c1d3efa61b3ff6b83` |
| Release Runway local foundation | 2026-08-22 — user approved revised exit | Installable tested Android skeleton, pushed repository, Codemagic app/workflow configured with remote Unity execution explicitly deferred, and store submission moved to the final Growth and release package |
| Task 6 — settings, accessibility and semantic input | 2026-08-22 — complete | Versioned atomic device-local settings; 20-control Frontend Settings surface; one project-wide semantic Input Router; composition-owned injection/reload/teardown; focused final gates `11/11` settings, `6/6` input, `11/11` assets, `11/11` controller, `1/1` motion, `12/12` bootstrap, `4/4` dependency/reload and `1/1` real launch; bounded critic PROCEED |
| Task 7 — versioned local saves and recovery | 2026-08-23 — complete | Schema-v1 story/Captain/discovery/cosmetic/Atlas/photo/birthday metadata; durable temporary write and validated backup rotation; explicit recovery results; ordered future migration registry; deterministic merge with typed player-choice conflicts; focused final gates `14/14` local save, `8/8` migration, `12/12` bootstrap and `1/1` real launch; bounded critic PROCEED |
| Task 8 — game modes and additive scene streaming | 2026-08-23 — complete | Exact guarded base-mode/overlay matrix and input/camera policies; version-1 Addressables scene catalogue at `jss.scene-catalog`; additive held activation, monotonic progress, exact-once cleanup, safe fallback and idempotent shutdown; permanent five-service bootstrap with no development initializer; focused final gates `7/7` modes, `2/2` catalogue, `12/12` streaming, `10/10` bootstrap and `1/1` real launch; all three bounded critic lifecycle findings reproduced and resolved |
| Task 9 — content IDs, typed events and editor validation | 2026-08-23 — complete | Immutable validated `ContentId`; six typed event payloads and snapshot-dispatch bus; deterministic contributor-based validation for duplicate IDs and every required reference/binding family; runtime events `4/4`, content validation `9/9`, full EditMode `262/262`, affected PlayMode Boot `8/8`, bootstrap `11/11` and cross-fixture `2/2`; CLI validator green; Android APK SHA-256 `3b10344f54aa82d853c8c5459b3f87950b594bf56a138f23b041fc7a27d79933`; Limrun/Argent Boot-to-Frontend, same-PID resume and root-Back exit smoke passed; bounded critic blocker reproduced and resolved |
| Runtime foundation QA remediation | 2026-08-23 — complete | JSS-011/JSS-012 promoted by the user; dependency-free tooling tests `11/11`; real isolated PlayMode manifest `90/90` across 10 Unity processes; one OS-locked inspector lease; bounded critic PROCEED |
| Task 10 — approved character reference sheets | 2026-08-23 — complete | All `12/12` required reference PNGs explicitly approved and hash-locked in `docs/art/character-reference-approval.md`; cinematic storybook style, cast scale, customization, individual turnarounds, expressions, equipment and material authority recorded; written semantic and dimensional contracts override incidental illustration errors in downstream builds |
| Task 11 — Blender source/export pipeline | 2026-08-23 — complete | Blender 5.2 scene setup, strict validation, deterministic LOD generation, transactional FBX/JSON export and scoped Unity import policy; Blender `13/13`, Unity importer `6/6`, real Generic primitive `.blend`→FBX round-trip, matching source/export hashes and staged Git LFS pointers; bounded critic PROCEED; no Task 12 hero asset produced |
| Task 12 Stage 0 — layered-2.5D production pivot | 2026-08-25 — user approved | Canonical 2.5D target hash-locked; surface and flight changed to authored 2.5D; character production changed to deterministic frame atlases; old 3D Stages 1–3 preserved as superseded history and Stage 4 stopped; implementation resumes at replacement Task 12 Stage 1 |
| Task 12 Stage 1 — layered 2.5D runtime proof | 2026-08-26 — complete | Eight-band URP 2D proof with independent parallax, `Rigidbody2D` surface movement, composition camera, real Captain/companion/Ori cutouts, touch controls and separate collision; focused final EditMode `7/7`, combined PlayMode `15/15`; three runtime camera-position captures; bounded harsh critic `PASS`; committed and pushed as `f99336333e6946ccd50c1ec43ffdb8b2aa3ac617` |
| Task 12 Stage 2 — deterministic sprite-atlas pipeline | 2026-08-26 — complete | Coherent-strip extraction, direction/baseline/alpha validation, deterministic atlas/manifest/evidence publication, fail-closed marker ownership, collision-safe sprite naming, scoped Unity import and manifest-driven playback; primitive round trip with 12 clean runtime frames; Python `13/13`, EditMode `6/6`, PlayMode `4/4`; bounded extreme critic `PASS` |
| Task 12 Stage 3 — modular Captain sprite families | 2026-08-27 — complete | Compact/Average/TallBroad sets; 8 motions × 2 facings × exactly 5 synchronized layers; palette masks, bounded modules and Stage 1 runtime integration; corrected modular jump/land; Python `6/6`, EditMode `4/4`, PlayMode `3/3`; 46-artifact runtime evidence manifest; bounded extreme critic `PASS` |
| Task 12 Stage 4 — bespoke crew and Ori sprite sets | 2026-08-27 — complete | Independently authored Mira/Juno/Kai/Bea sets plus mechanical Ori; 8 clips × 2 facings each; stable live anchors and deterministic manifests; corrected Juno interaction scale and Bea scan; Python `3/3`, EditMode `3/3`, PlayMode `1/1`; 96-frame both-facing Unity reel; bounded extreme critic `PASS` |
| Task 12 Stage 5 — final Mirra production proof | 2026-08-28 — user approved | Eight independently owned final-art bands; accepted Captain/crew/Ori sprites; bounded recovery, interaction and Lens target; lights, masks, particles, grading and restrained HUD; full EditMode `293/293`, isolated PlayMode `114/114`, Lens correction `1/1`; critic `PASS`; exact Android APK SHA-256 `6e9597e0f1ae498d0fdf0f6d46d6032279b150f29e4a6906ebb09da4ecbd9d8d`; exact-device visual approved with the video limitation recorded honestly |
| Task 12 — approved 2.5D production foundation | 2026-08-28 — complete | Replacement Stages 0–5 complete: canonical visual target, layered runtime, deterministic atlas pipeline, three modular Captain families, bespoke crew/Ori sets and final-art Mirra Android acceptance route. User approved the final graphical result; benchmark ceilings live in `docs/qa/task12-mirra-benchmark.md`; superseded 3D WIP remains preserved pending JSS-013 |
| Task 13 — production surface motor and composition camera | 2026-08-29 — complete | Production prefab/body calibrations, fixed-step movement, environmental acceleration and platform inheritance, authored camera profiles, real touch route and corrected mobile Captain presentation. Focused gates: prefab `2/2`, motor `5/5`, camera `4/4`, lifecycle `2/2`, affected Mirra EditMode `5/5`, affected Mirra PlayMode `3/3`, touch route `1/1`, final Captain package Python `6/6`, importer `6/6`, renderer `3/3`. The user rejected the first lower-leg correction evidence, so it is explicitly superseded. The final connected-leg Android APK SHA-256 is `9c8652adcdd8dbc315e9c3a58cedcb3ba4191449a2acbc02f481617b59e14d12`; its exact-device stride evidence proves both legs and boots remain attached and visible and received fresh harsh-critic `PASS`. |
| Task 14 — contextual interactions and anchor reservations | 2026-08-30 — complete | Typed Player/Crew/Ori anchor definitions and clips; deterministic distance/facing/layer/depth/mode/tool selection; exclusive and shared leases; cancellation, active timeout, peer-fault cancellation, recovery and exact release. Authentic critic REDs covered duplicate actor clips, a faulted actor with blocked peers and lease timeout; fresh focused EditMode `5/5` and PlayMode `5/5` pass with exit 0; bounded harsh critic `PROCEED`. |
| Task 15 — Crew Director and personality brains | 2026-08-30 — complete | Exactly two human companions plus Ori; authored personality attentions; strict story/safety/personality priority; formation, dialogue-token and interaction-reservation arbitration; all 12 crew states; declared 2D traversal graph; hidden recovery; bounded five-Hz decisions. Authentic critic REDs covered disconnected arbitration, unused traversal, unconstrained cadence/team input, visible recovery and non-transitive near-tie selection; fresh focused EditMode `11/11` and PlayMode `8/8` pass with exit 0; bounded harsh critic `PROCEED`. |
| Task 16 — Discovery Lens and evidence gameplay | 2026-08-30 — complete | Six exact Lens modes; authored instrument/phenomenon compatibility; distinct Point/Track/Region focus; serializable recoverable predictions and evidence; typed discovery events; real Mirra touch aiming, mode, SCAN, reticle and progress route; transition-safe teardown. Authentic critic REDs covered dead focus behavior, unreachable production evidence, absent presentation and in-flight teardown; final focused gates `8/8`, `6/6`, `3/3` and `2/2` pass with exit 0; bounded harsh critic `PROCEED`. |
| Task 17 — 2.5D flight, assists, landing and recovery | 2026-08-30 — complete | Deterministic bounded-plane flight; boost, brake, drift, momentum and three assist profiles; authored depth lanes, gravity opportunity, prediction, checkpoint recovery and transactional landing; layered homemade ship sprites and 90-second production route. Fresh focused EditMode `10/10`, PlayMode `9/9`, post-await rollback `1/1` and real Boot idempotence `1/1` pass; bounded harsh critic `PROCEED`; exact Android APK SHA-256 `3193394f66aaae95b2c181ad4d1b98cae3367e38e56bada37eb35aa5b48fc69b`; exact-device route reached the real Mirra surface and all paid instances/tunnels were closed. |
| Task 18 — mission graph, dialogue, hints and Cosmic Atlas | 2026-08-30 — complete | Deterministic authored mission graph; exact typed-event payloads; automatic story dialogue with terminal-only chain completion; behavior-only hints; graph-authentic restart checkpoints and recovery; migration-safe paired discovery/Atlas progression; localized three-depth Atlas text and claim-specific NASA provenance. Authentic critic REDs covered six integration faults plus early-hint regression; final focused gates mission `7/7`, dialogue `7/7`, Atlas/validation `4/4`, real route `1/1`, project validator green; bounded extreme critic `PROCEED`; implementation commit `c23f1ff`. |
| Task 19 — mechanics-complete Mirra vertical slice | 2026-08-31 — complete | Real Flight-to-Mirra landing and surface route; hot/cold climate fields; durable objective/fragment presentation; live Mira/Juno/Ori crew runtime; reserved Captain/Juno/Ori repair; Lens evidence and Atlas unlock; retryable transactional departure. Fresh final `MirraMissionTests` pass `11/11`; Android APK SHA-256 `c4e3b2e2ac527f587086d81ae1bd13df5ee9e8a784790b281532495f83955d0c`; bounded exact-device presentation/resume spot-check; bounded extreme critic `PROCEED`. |
| Task 20 — mobile quality benchmark and scalable rendering | 2026-08-31 — complete | Four device-local quality profiles; bounded Mirra lights, particles, parallax and volume; live Shader Graph normal/surface/palette paths; focused gates `3/3`, `2/2` and `5/5`; Android APK SHA-256 `ebce83c1a4fe13ab4efc5b8edc586d40d69e1e01ebd0d3b3f351fdd71afadb5c`; exact-device Performance sample with zero intervals over 40 ms; bounded extreme critic `PROCEED`. |
| Task 21 — optional account/cloud local foundation | 2026-08-31 — complete locally; external activation tracked as JSS-021 | Durable guest identity; conflict-aware local-first orchestration with atomic story/mission branch choice plus safe field merges; checkpoint-driven optional sync with nonfatal cloud-failure containment; explicit revision-zero CAS; rule-compatible UID-scoped projection; export/real unlink/delete; strict Firestore rules; truthful Frontend states and post-auth recovery; official pinned Firebase packages with upstream receipt; Android OS-backup exclusions; focused merge `7/7`, package/privacy `5/5`, Frontend `1/1` + `1/1`, account `13/13`, controller `11/11`, dependencies `5/5`, installer `10/10`, launch `1/1`, rules `4/4`; final critic `PROCEED`; APK SHA-256 `3fe9fdebfb4d4b14f9fe243d57fde495fdfbc8a62776b5635cf3f14732515ef9`; device-computed installed bytes and no-config cold-start/resume/exit passed, and all Task 21 paid-run resources were removed. Credentialed cloud activation is not claimed. |
| Task 22 — private birthdays and annual gifts | 2026-08-31 — complete locally; external activation tracked as JSS-021 | Private day/month/year and age-band rules; exact leap-day and 30-UTC-date claim semantics; serialized guest updates; one correction plus grown-up confirmation; migration-safe schema-v3 cloud projection; monotonic correction/gift history; App-Check/auth/server-time annual grant; server-authoritative profile creation and account deletion boundary; client-immutable claim ledger; scene-independent Clubhouse/Ori/cosmetic identities. Fresh focused gates: birthday `11/11`, affected save/migration/cloud `32/32`, account lifecycle `13/13`, Functions `6/6`, Firestore Emulator rules `5/5`; bounded extreme critic `PROCEED`. No Firebase deployment, credentialed integration, finished Clubhouse scene or final cosmetic art is claimed. |
| Task 23 — RevenueCat Test Store and Google Play commerce | 2026-08-31 — complete locally; credentialed activation tracked as JSS-023 | Provider-neutral optional commerce, verified identity-scoped entitlement cache, explicit restore and grown-up purchase gate, official pinned RevenueCat Unity SDK, transactional build-key injection, `singleTop` purchase return, and package-derived Google/Galaxy dependency isolation. Fresh focused gates: entitlement/cache `6/6`, shop `13/13`, build-key/manifest/variant `4/4`, affected build integration `35/35`, bootstrap `10/10`; bounded extreme critic `PROCEED`; no-key internal APK SHA-256 `c3fc001c7c906019be818611b3d92bdc65a98208048d34171396e842af591c15`. No dashboard product creation, signed AAB, licensed purchase or RevenueCat transaction is claimed. |
| Task 24 — isolated Samsung Galaxy commerce | 2026-08-31 — complete locally; external activation tracked as JSS-024 | Samsung IAP 6.5.2 Galaxy-only module/facade; zero RevenueCat/BillingClient Galaxy Gradle graph; production-only build/runtime mode; trusted-verifier-first grant ordering; identity-bound atomic pending/authority/acknowledgement recovery; explicit Restore adoption. Authentic correction RED `6/10`; source-final isolation/runtime `10/10`; affected build configuration `4/4`; bounded extreme critic `PROCEED`. No Samsung license acceptance, Seller Item creation, verifier deployment, signed Galaxy artifact or physical transaction is claimed. |
| Task 25 — Koro/Vesper and second Signal fragment | 2026-09-01 — complete locally; corrected exact-APK completion spot-check tracked as JSS-025 | Production Vesper flight/landing into full-bleed layered Koro; retry-safe spectra comparison; natural/Signal geyser rhythm; depth-specific Atlas copy; Bea/Mira/Ori observations; durable Mirra and Koro fragments. Source-final production E2E and fixture pass `1/1` and `10/10`; corrected APK SHA-256 `c7f55a58f8ea14b38c935616905fd440b8869e695a0066b96b4b0396fde735ca`; bounded extreme critic `PROCEED`. |

## Resolved or promoted findings

Move an entry here only when its owner task starts or the user explicitly
promotes it. Record the resolving task or commit instead of deleting history.

| ID | Resolution | Evidence |
|---|---|---|
| JSS-011 | Resolved by the 2026-08-23 runtime-foundation QA remediation. Full PlayMode verification now uses the source-validated fixture manifest and one Unity process per fixture; the invalid aggregate is no longer a gate. | Strict runner summary: 10 fixtures and `90/90` selected tests passed; stale/missing/malformed/zero-test reports fail closed. |
| JSS-012 | Resolved by the 2026-08-23 runtime-foundation QA remediation. Limrun supplies the emulator/install/tunnel while an identity-bound lease gives exactly one UI-inspection backend ownership. | Cross-process tests prove competing Argent/Limrun-UIAutomator claims cannot both succeed and guarded commands retain the OS lock through completion. |
| JSS-014 | Resolved by replacement Task 12 Stages 3 and 4. The Captain families and every bespoke crew/Ori actor now use coherent frame-atlas animation in both facings instead of the Stage 1 static proof cutouts. | Stage 3 Python `6/6`, EditMode `4/4`, PlayMode `3/3`; Stage 4 Python `3/3`, EditMode `3/3`, PlayMode `1/1`; both stages received final extreme-critic `PASS`. |
| JSS-015 | Resolved by replacement Task 12 Stage 5. The final Mirra scene owns one bounded `SurfaceRecovery2D` authority and safe respawn contract instead of allowing indefinite falls. | `Mirra2DAssetValidationTests` and `Mirra2DProofTests` pass inside the final EditMode `293/293` and PlayMode `114/114` package-boundary suites. |
| JSS-016 | Resolved by Task 13. The accepted action capture visibly proves run, Jump, `SIGNAL LINKED` interaction and Lens response. The user correctly rejected `final-lower-leg-run-v2.mp4`: translating the full leg detached it from the pelvis. An authentic pixel-connectivity RED reproduced the defect; the generator now preserves each hip pivot and produces swing clearance by scaling the leg around that pivot, with boot anchors using the identical transform. | Prior complete action clip `final-captain-actions-corrected.mp4`; rejected correction clip `final-lower-leg-run-v2.mp4` SHA-256 `0a2b13bbbc731af580477ff03f8bd71ee2f85392cf68f092f24a2685070516c3`; final APK SHA-256 `9c8652adcdd8dbc315e9c3a58cedcb3ba4191449a2acbc02f481617b59e14d12`; accepted runtime excerpt `final-connected-stride-runtime.mp4` SHA-256 `cb23adc61887171edc0e7443a80e22d696205d9a1c6a0ab442a6bbf80326d008`; package Python `6/6`, importer `6/6`, renderer `3/3`; fresh harsh-critic `PASS`; filtered final-process log had no fatal/unhandled/managed-exception match. |
| JSS-019 | Resolved at the Task 19 delivery checkpoint. The already-authorized branch destination and payload passed the execution platform's trust review, so normal scoped pushes are available again. | Task 19 commit `9ae01d9b206074ab86a7203afaee7372ad563250` was pushed and the local/remote branch refs matched exactly. |
| JSS-020 | Task 20's corrected exact-APK hosted Android Performance sample measured total PSS 865,920 KB, 128,087 KB (17.36%) above the older Task 12 observation. The available gate cannot attribute the post-baseline growth safely. | Task 30 broad performance/device pass: capture Unity Memory Profiler plus GPU/overdraw evidence, attribute Tasks 13–20 growth, then reduce or explicitly approve the release-device budget before store submission. |
| JSS-004 | Resolved by Task 21. The stale `NO ONLINE SERVICES` footer was replaced with state-derived offline/available/linked/unavailable account copy, and the checked-in no-config build describes only local progress and unavailable backup. | Frontend asset, controller, dependency-injection and launch tests reject the stale literal and cover truthful state transitions. |
